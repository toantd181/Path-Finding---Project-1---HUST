class CarModeTool:
    """Manages the state for the car mode (single edge block) tool."""
    def __init__(self):
        # This tool primarily signals activation.
        # The core logic for finding the nearest edge and blocking it
        # will reside in MainWindow and MapViewer.
        print("CarModeTool initialized.")

    # Add methods if specific settings are needed later (e.g., type of block)